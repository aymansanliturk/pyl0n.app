node_modules/
dist/
dist-src/
# libs/ and vendor/ are NOT ignored — both are committed for offline/browser use
# Keep build/ icon assets — do NOT ignore build/
# azure-config.json is committed as a template (with placeholder values only)
# Never commit the real clientId/tenantId — edit the file after installing
